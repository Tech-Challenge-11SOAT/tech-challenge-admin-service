apiVersion: apps/v1
kind: Deployment
metadata:
  name: tech-challenge-admin-service
  namespace: techchallenge
spec:
  replicas: 2
  selector: 
    matchLabels: 
      app: admin-service 
  strategy:
    type: RollingUpdate
    rollingUpdate:
        maxSurge: 1
        maxUnavailable: 0
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: nicolasmartinss/admin-service:${{ github.sha }} 
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: admin-service-config 
            - secretRef:
                name: admin-service-secret 
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1024Mi
          livenessProbe:
            httpGet:
              port: 8080
              path: /health
            initialDelaySeconds: 5
            periodSeconds: 20
            failureThreshold: 3
            successThreshold: 1
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              port: 8080
              path: /health
            initialDelaySeconds: 3
            periodSeconds: 4
            failureThreshold: 3
            successThreshold: 2
            timeoutSeconds: 5
          startupProbe:
            httpGet:
              port: 8080
              path: /health
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5